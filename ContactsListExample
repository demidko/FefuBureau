# It`s comment line.
# FEFU Contacts DSL guide:

#     Основная сущность - Объект.

#     У объекта может быть имя.
#     В одной области видимости не может быть двух объектов с одинаковым именем.

#     У каждого объекта непременно должно быть значение.
#     Значение может быть либо строкой ограниченной символами "", либо набором других объектов ограниченным символами {}
#     Набор объектов не может быть пустым.
#     Если у объекта есть и имя и значение, то он обозначается следующим образом
#        <ObjectName>: <ObjectValue>
#        Например объект с именем FirstObject и значением в виде строки:
            FirstObject: "This is a string"
#        Или объект с именем SecondObject и значением в виде набора из двух именованных объектов, 
#        один из которых также является набором объектов, но без имени:
            SecondObject: { 
                a: "string" 
		b: {"0", "1", "2"} 
            }
#     Если в определенной области видимости хотя бы у одного объекта есть имя, у остальных оно должно быть тоже.

{
	Name: "Иван Иванович Иванов"	
	Post: "Ректор Воображаемого Университета"
	Phone: "8-914-038-10-15"
	Email: "ivan_ivanovich@example.com"
	School: "Школа управления Галактикой"
	Departament: "Кафедра математических методов в сферическом вакууме"
} {
        Name: "Пётр Васильевич Сидров"
	Post: "Тестер"
	Phone: "8-924-458-70-05"
	Email: "vf_vasya_32_54_14j@egmail.com"
	School: "Школа цифрового распила"
	Departament: "Кафедра быдлокодинга"
}
